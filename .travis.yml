sudo:
    - false
language:
    - node_js
node_js:
    - "0.10"
branches:
    only:
        - release
cache:
    directories:
        - node_modules
after_success:
    - cat coverage/lcov.info | coveralls
